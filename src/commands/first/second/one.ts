import type { CommandModule } from "types";

const command: CommandModule = [
    "slash",
    {
        "description": "퐁"
    },
    async ({ interaction }) => {
        
    }
];

export default command;
